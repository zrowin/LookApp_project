Purpose: Folder for request handlers / controllers.

Example structure:
- `controllers/auth.js` — auth routes and logic
- `controllers/looks.js` — endpoints for creating and retrieving looks

Notes:
- Keep controllers thin; move business logic to `services/`.
